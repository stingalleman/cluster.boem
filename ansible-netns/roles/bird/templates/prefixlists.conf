#jinja2: lstrip_blocks:True
{% for prefixlist in prefixlists %}
define {{ prefixlist.name }} = [
  {% for prefix in prefixlist.prefixes %}
  {{ prefix }}{% if not loop.last %},{% endif %}

  {% endfor %}
];
{% endfor %}
