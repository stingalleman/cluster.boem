apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget-volume
  namespace: default
  labels:
    app: actual-budget-volume
spec:
  # storageClassName:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
