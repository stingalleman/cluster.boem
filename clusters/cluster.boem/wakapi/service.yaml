apiVersion: v1
kind: Service
metadata:
  name: wakapi
  namespace: default
spec:
  selector:
    app: wakapi
  type: ClusterIP
  ports:
    - name: wakapi
      protocol: TCP
      port: 80
      targetPort: 3000
