apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mino-operator
  namespace: minio
spec:
  chart:
    spec:
      chart: minio-operator
      sourceRef:
        kind: HelmRepository
        name: operator
        namespace: flux-system
      version: "6.0.3"
  interval: 24h
