apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: dwrg-redirect
  namespace: default
spec:
  redirectRegex:
    permanent: true
    regex: ^.+
    replacement: https://dwrg.nl
