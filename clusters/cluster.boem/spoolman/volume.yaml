apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spoolman-data
  namespace: default
  labels:
    app: spoolman
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
