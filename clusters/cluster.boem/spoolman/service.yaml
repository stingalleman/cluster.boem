apiVersion: v1
kind: Service
metadata:
  name: spoolman
  namespace: default
spec:
  selector:
    app: spoolman
  type: ClusterIP
  ports:
    - name: spoolman
      protocol: TCP
      port: 8000
      targetPort: 8000
