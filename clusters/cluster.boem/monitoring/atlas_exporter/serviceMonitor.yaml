apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: atlas-exporter
  namespace: monitoring
  labels:
    release: monitoring-stack
spec:
  jobLabel: atlas-exporter
  selector:
    matchLabels:
      app: atlas-exporter
  namespaceSelector:
    matchNames:
      - monitoring
  endpoints:
    - port: atlas-exporter
      interval: 1m
