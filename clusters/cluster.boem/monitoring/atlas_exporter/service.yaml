apiVersion: v1
kind: Service
metadata:
  name: atlas-exporter
  namespace: monitoring
spec:
  selector:
    app: atlas-exporter
  type: ClusterIP
  ports:
    - name: atlas-exporter
      protocol: TCP
      port: 80
      targetPort: http
