apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: matrix-stack-fork
  namespace: flux-system
spec:
  interval: 10m
  url: oci://ghcr.io/stingalleman/ess-helm/matrix-stack
  provider: generic
  secretRef:
    name: ghcr-auth
  ref:
    semver: "26.1.3"
