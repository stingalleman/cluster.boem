apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pve-redirect
  namespace: traefik
spec:
  redirectRegex:
    permanent: true
    regex: ^.+
    replacement: https://pve.i.as216455.net
