apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: dns-udp-ingress
  namespace: traefik
spec:
  entryPoints:
    - dns_udp
  routes:
    - match: HostSNI(`*`)
      services:
        - name: nameserver
          namespace: dns
          port: 53
