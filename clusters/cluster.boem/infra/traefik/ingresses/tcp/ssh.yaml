apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: ssh-ingress
  namespace: traefik
spec:
  entryPoints:
    - shh
  routes:
    - match: HostSNI(`*`)
      services:
        - name: forgejo-ssh
          namespace: forgejo
          port: ssh
