apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: sporten-monitor
  namespace: default
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: sporten
  podMetricsEndpoints:
    - port: metrics
