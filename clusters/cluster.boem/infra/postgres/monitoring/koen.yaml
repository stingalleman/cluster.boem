apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: koen-monitor
  namespace: cnpg-system
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: koen
  podMetricsEndpoints:
    - port: metrics
