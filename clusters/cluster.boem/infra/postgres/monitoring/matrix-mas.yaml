apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: matrix-mas-monitor
  namespace: cnpg-system
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: matrix-mas
  podMetricsEndpoints:
    - port: metrics
