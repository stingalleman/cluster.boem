apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: discord-monitor
  namespace: cnpg-system
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: discord
  podMetricsEndpoints:
    - port: metrics
