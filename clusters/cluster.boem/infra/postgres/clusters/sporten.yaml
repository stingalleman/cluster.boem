apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: sporten
  namespace: default
spec:
  instances: 2
  primaryUpdateStrategy: unsupervised
  enableSuperuserAccess: false

  storage:
    size: 5Gi
    storageClass: ceph-rbd-pve
