apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: immich
  namespace: cnpg-system
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: immich-postgres
  podMetricsEndpoints:
    - port: metrics
