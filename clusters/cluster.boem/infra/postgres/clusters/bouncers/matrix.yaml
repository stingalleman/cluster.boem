apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: pooler-matrix-rw
  namespace: cnpg-system
spec:
  cluster:
    name: matrix

  instances: 3
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "10"
      query_wait_timeout: "0"
