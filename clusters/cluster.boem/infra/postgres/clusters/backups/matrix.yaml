apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: matrix-backup
spec:
  cluster:
    name: matrix
  schedule: "0 0 0 * * *"
  backupOwnerReference: selfa
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
