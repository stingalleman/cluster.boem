apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: matrix-mas-backup
spec:
  cluster:
    name: matrix-mas
  schedule: "0 0 0 * * *"
  backupOwnerReference: selfa
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
