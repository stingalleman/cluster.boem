apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth.as216455.net
  namespace: traefik
spec:
  secretName: auth.as216455.net-prod-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "auth.as216455.net"
  dnsNames:
    - "auth.as216455.net"
