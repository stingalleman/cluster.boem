apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: headlamp
  namespace: kube-system
spec:
  chart:
    spec:
      chart: headlamp
      # version: "1.18.0"
      sourceRef:
        kind: HelmRepository
        name: headlamp
        namespace: flux-system
  interval: 24h
  values: