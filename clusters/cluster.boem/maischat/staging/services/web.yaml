apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: maischat-staging
spec:
  selector:
    app: web
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
