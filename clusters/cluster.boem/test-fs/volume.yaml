apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fs-test
  namespace: default
  labels:
    app: fs-test
spec:
  storageClassName: ceph-block
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: 20Gi
