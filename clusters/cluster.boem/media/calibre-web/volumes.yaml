apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-data
  namespace: media
  labels:
    app: calibre-web
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
