apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-downloads
  namespace: default
  labels:
    app: media-downloads
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
