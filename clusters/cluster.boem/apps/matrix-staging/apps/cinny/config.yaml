kind: ConfigMap
apiVersion: v1
metadata:
  name: cynisch-config
  namespace: matrix-staging
data:
  config.json: |
    {
      "defaultHomeserver": 0,
      "homeserverList": [
        "maishond.nl",
        "staging.matrix.maishond.nl",
        "vdaa.me"
      ],
      "allowCustomHomeservers": false,

      "featuredCommunities": {
        "openAsDefault": false,
        "spaces": [
        ],
        "rooms": [
        ],
        "servers": ["staging.matrix.maishond.nl", "maishond.nl"]
      },

      "hashRouter": {
        "enabled": false,
        "basename": "/"
      }
    }
