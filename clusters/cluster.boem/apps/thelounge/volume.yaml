apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thelounge-data
  namespace: default
  labels:
    app: thelounge
spec:
  storageClassName: ceph-rbd-pve
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: 10G
