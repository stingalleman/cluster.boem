apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-thumbs
  namespace: immich
spec:
  storageClassName: proxmox-ssd-ext4
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
#
