apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-upload
  namespace: immich
spec:
  storageClassName: proxmox-hdd-ext4
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
