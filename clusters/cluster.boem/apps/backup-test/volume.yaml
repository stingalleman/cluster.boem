apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-volume
  namespace: test-backup
  labels:
    app: test-app
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
