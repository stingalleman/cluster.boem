apiVersion: v1
kind: Service
metadata:
  name: mediamtx
  namespace: default
spec:
  selector:
    app: mediamtx
  type: LoadBalancer
  ports:
    - name: rtsp
      protocol: TCP
      port: 8554
      targetPort: rtsp
