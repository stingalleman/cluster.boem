apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: library
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: cephfs-hdd
  resources:
    requests:
      storage: 2T
