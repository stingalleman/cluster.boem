apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-profilarr
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-block
  resources:
    requests:
      storage: 5G
