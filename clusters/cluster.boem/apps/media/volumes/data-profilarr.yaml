apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-profilarr
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-pve
  resources:
    requests:
      storage: 5G
