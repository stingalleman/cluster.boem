apiVersion: v1
kind: Service
metadata:
  name: maishond-site
  namespace: default
spec:
  selector:
    app: maishond-site
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
