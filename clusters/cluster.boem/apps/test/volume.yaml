apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-data
  namespace: default
  labels:
    app: test-data
spec:
  storageClassName: proxmox-ssd-ext4
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
