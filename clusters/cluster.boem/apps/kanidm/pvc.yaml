apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kanidm-storage
  namespace: kanidm
  labels:
    app: kanidm-storage
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
