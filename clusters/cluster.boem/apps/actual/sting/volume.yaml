apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget
  namespace: default
  labels:
    app: actual-budget
spec:
  storageClassName: ceph-rbd-pve
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
