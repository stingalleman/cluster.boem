apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget-wouter-volume3
  namespace: default
  labels:
    app: actual-budget-wouter
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
