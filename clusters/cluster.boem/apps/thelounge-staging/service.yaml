apiVersion: v1
kind: Service
metadata:
  name: thelounge-staging-http
  namespace: default
spec:
  selector:
    app: thelounge-staging
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 9000
      targetPort: 9000
