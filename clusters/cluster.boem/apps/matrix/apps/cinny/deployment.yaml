apiVersion: apps/v1
kind: Deployment
metadata:
  name: cynisch
  namespace: matrix
  labels:
    app: cynisch
spec:
  selector:
    matchLabels:
      app: cynisch
  replicas: 1
  template:
    metadata:
      labels:
        app: cynisch
    spec:
      imagePullSecrets:
        - name: ghcr-login-secret
      containers:
        - name: cynisch
          image: ghcr.io/maishond/cynisch:7c3360957c487e98ae6e34dc8571ee3fb4853b37
          ports:
            - containerPort: 80
              name: http
          volumeMounts:
            - name: config
              mountPath: /usr/share/nginx/html/config.json
              readOnly: true
              subPath: config.json
      volumes:
        - name: config
          configMap:
            name: cynisch-config
            defaultMode: 0444
            items:
              - key: config.json
                path: config.json
      restartPolicy: Always
