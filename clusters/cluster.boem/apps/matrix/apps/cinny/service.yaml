apiVersion: v1
kind: Service
metadata:
  name: cinny
  namespace: matrix
spec:
  selector:
    app: cinny
  type: ClusterIP
  ports:
    - name: cinny
      protocol: TCP
      port: 80
      targetPort: http
