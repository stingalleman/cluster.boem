apiVersion: v1
kind: Service
metadata:
  name: docker-bridge
  namespace: matrix
spec:
  selector:
    app: docker-bridge
  type: ClusterIP
  ports:
    - name: docker-bridge
      protocol: TCP
      port: 29334
      targetPort: http
