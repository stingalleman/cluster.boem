apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-bridge-db
  namespace: matrix
  labels:
    app: docker-bridge
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
