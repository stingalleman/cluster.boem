apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-data
  namespace: matrix
  labels:
    app: synapse
spec:
  # storageClassName: nfs-client
  # accessModes:
  # - ReadWrite
  resources:
    requests:
      storage: 5Gi
