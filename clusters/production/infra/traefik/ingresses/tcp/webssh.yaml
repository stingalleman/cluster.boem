apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: webssh-ingress
  namespace: traefik
spec:
  entryPoints:
    - webssh
  routes:
    - match: HostSNI(`*`)
      services:
        - name: forgejo-ssh
          namespace: forgejo
          port: ssh
