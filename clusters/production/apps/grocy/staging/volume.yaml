apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy-config-staging
  namespace: grocy
  labels:
    app: grocy
spec:
  storageClassName: ceph-rbd-pve
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
