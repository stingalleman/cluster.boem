function set_local_pref(int local_pref) {
  bgp_local_pref = local_pref;
}

function import_default_inet6() {
  # check RPKI
  # if is_v6_rpki_invalid() then {
  #   print "Reject: RPKI invalid, net: ", net;
  #   reject;
  # }

}

function test(string input) {
  print input;
}

function export_default_inet6() {
  # check RPKI
  # if is_v6_rpki_invalid() then {
  #   print "Reject: RPKI invalid, net: ", net;
  #   reject;
  # };

  # check if prefix matches prefixlists
  if net ~ as216455_inet6 then {
    print "exporting ", net;
    accept;
  }

  reject;
}
