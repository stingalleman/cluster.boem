protocol static {
  ipv6 { export all; };
{% for route in bird_extra_route6 | default([]) %}
  route {{ route.subnet }} blackhole;
{% endfor %}
}

protocol static {
  ipv4 { export all; };
{% for route in bird_extra_route4 | default([]) %}
  route {{ route.subnet }} blackhole;
{% endfor %}
}